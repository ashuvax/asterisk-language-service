same => n(OneTest),Set(IVR_CONF_GLOBAL_test=1)
same => n,Set(IVR_CONF_GLOBAL_test=2)
same => n,AGI(agi://fagi-server/TrimWavSilence?prefix=${Record})

; if variable WavTestStatus is set to 1, then play the file, else go to OneTest
same => n,GotoIf($["${WavTestStatus}" = "1"]?OneTest)
same => n,Playback(${Record})