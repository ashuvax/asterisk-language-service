[globals]

[default]
exten => s,1,Answer()
same => n,Wait(1)
same => n,Playback(hello-world)
same => n,Hangup()

[test]
exten => 123,1,Answer()
same => n,Dial(SIP/1234)
same => n,Playback(hello-world
same => n,Hangup()